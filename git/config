[user]
  email = jakob.unfried@tum.de
  name = Jakob Unfried
  signingkey = E5726847

[github]
  user = Jakob-Unfried

[init]
  templatedir = ~/.dotfiles/git/template

[core]
  excludesfile = ~/.dotfiles/git/ignore
  autocrlf = input

[gpg]
  program = /opt/homebrew/bin/gpg

[commit]
  gpgsign = true

[push]
  default = current

[pull]
  rebase = true

[rebase]
  autostash = true

[advice]
  statusHints = false

[alias]
  hist = log --pretty=format:'%C(yellow)[%ad]%C(reset) %C(blue)[%h]%C(reset) | %C(white)%s %C(dim white){{%an}}%C(reset) %C(auto)%+d%C(reset)' --graph --date=relative --color=always
  stashs = stash push --staged
